"""archtrace test suite.

Package-ified so `unittest discover` can be driven with an explicit
`top_level_dir`, which the coverage gate needs in order to trace the package
rather than the test runner.
"""
